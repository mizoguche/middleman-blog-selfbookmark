require "spec_helper"

describe Middleman::Blog::Selfbookmark do
  it "has a version number" do
    expect(Middleman::Blog::Selfbookmark::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
